{
  "name": "cfsm-theme-butterfly",
  "private": true,
  "version": "0.1.0",
  "description": "Komari-Butterfly (WinUI 3 / Mica style) ported to CF-Server-Monitor as a self-hosted third-party theme.",
  "repository": {
    "type": "git",
    "url": "https://github.com/LucaLin233/cfsm-theme-butterfly.git"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run build && npm run validate"
  },
  "engines": {
    "node": ">=20"
  }
}
